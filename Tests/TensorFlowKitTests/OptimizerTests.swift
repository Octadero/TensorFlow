import XCTest

import CAPI
import Proto
import CTensorFlow
import TensorFlowKit


class OptimizerTests: XCTestCase {
    func testBase() {
        do {
            
        } catch {
            XCTFail(error.localizedDescription)
        }
    }
    static var allTests = [
        ("testBase", testBase)
    ]
}
